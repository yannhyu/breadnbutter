
Prefixes:
     ns0: http://www.webserviceX.NET/
     xsd: http://www.w3.org/2001/XMLSchema

Global elements:
     
     ns0:GetQuote(symbol: xsd:string)
     ns0:GetQuoteResponse(GetQuoteResult: xsd:string)
     ns0:string(xsd:string)

Global types:
     
     xsd:ENTITIES
     xsd:ENTITY
     xsd:ID
     xsd:IDREF
     xsd:IDREFS
     xsd:NCName
     xsd:NMTOKEN
     xsd:NMTOKENS
     xsd:NOTATION
     xsd:Name
     xsd:QName
     xsd:anySimpleType
     xsd:anyURI
     xsd:base64Binary
     xsd:boolean
     xsd:byte
     xsd:date
     xsd:dateTime
     xsd:decimal
     xsd:double
     xsd:duration
     xsd:float
     xsd:gDay
     xsd:gMonth
     xsd:gMonthDay
     xsd:gYear
     xsd:gYearMonth
     xsd:hexBinary
     xsd:int
     xsd:integer
     xsd:language
     xsd:long
     xsd:negativeInteger
     xsd:nonNegativeInteger
     xsd:nonPositiveInteger
     xsd:normalizedString
     xsd:positiveInteger
     xsd:short
     xsd:string
     xsd:time
     xsd:token
     xsd:unsignedByte
     xsd:unsignedInt
     xsd:unsignedLong
     xsd:unsignedShort

Bindings:
     HttpGetBinding: {http://www.webserviceX.NET/}StockQuoteHttpGet
     HttpPostBinding: {http://www.webserviceX.NET/}StockQuoteHttpPost
     Soap11Binding: {http://www.webserviceX.NET/}StockQuoteSoap
     Soap12Binding: {http://www.webserviceX.NET/}StockQuoteSoap12

Service: StockQuote
     Port: StockQuoteSoap (Soap11Binding: {http://www.webserviceX.NET/}StockQuoteSoap)
         Operations:
            GetQuote(symbol: xsd:string) -> GetQuoteResult: xsd:string

     Port: StockQuoteSoap12 (Soap12Binding: {http://www.webserviceX.NET/}StockQuoteSoap12)
         Operations:
            GetQuote(symbol: xsd:string) -> GetQuoteResult: xsd:string

     Port: StockQuoteHttpGet (HttpGetBinding: {http://www.webserviceX.NET/}StockQuoteHttpGet)
         Operations:
            GetQuote(symbol: xsd:string) -> xsd:string

     Port: StockQuoteHttpPost (HttpPostBinding: {http://www.webserviceX.NET/}StockQuoteHttpPost)
         Operations:
            GetQuote(symbol: xsd:string) -> xsd:string

